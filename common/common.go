package common

import "os"

// Getenv get env vars or fallback if not available
func Getenv(key, fallback string) string {
	value := os.Getenv(key)
	if len(value) == 0 {
		return fallback
	}
	return value
}
